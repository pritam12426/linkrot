# linkrot

A fast, dependency-light CLI that recursively scans a directory tree for broken symbolic links, then lets you list, dry-run, or permanently delete them — with glob filtering and a pluggable delete command for tools like `trash`.
